apiVersion: v1
kind: Secret
metadata:
  name: authentik-oauth2-client
  namespace: default
type: Opaque
stringData:
  # client_id of the authentik OAuth2 provider used for the exchange.
  client-id: "CHANGE_ME"
  # client_secret of the same provider.
  client-secret: "CHANGE_ME"
